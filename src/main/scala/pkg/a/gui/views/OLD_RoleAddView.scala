package pkg.a.gui.views

import scalafx.application.JFXApp3
import scalafx.scene.Scene
import scalafx.scene.control.{Button, Label, TextField}
import scalafx.scene.image.Image
import scalafx.scene.layout.{BorderPane, StackPane}
import scalafx.scene.paint.Color
import scalafx.scene.paint.Color.*

import scala.swing.GridBagPanel
//import scalafx.scene.shape.Rectangle
import scalafx.geometry.Insets
import scalafx.scene.layout.*

/*

//object RoleAddView extends JFXApp3:
object RoleAddView:
  def apply(onExit: () => Unit = () => ()): VBox =

    val roleLbl = new Label("Ruolo")
    val roleFld = new TextField()

    val descriptionLbl = new Label("Descrizione")
    val descriptionFld = new TextField()

    val saveBtn = new Button("Salva")
    val clearBtn = new Button("Pulisci")
    val exitBtn = new Button("Esci")

  //  new GridBagPanel:

    new VBox:
      spacing = 15
      padding = Insets(20)
      children = Seq(roleLbl, roleFld)

//  override def start(): Unit =
/*
    stage = new JFXApp3.PrimaryStage:
      title.value = "Aggiunta Ruolo"
      // resizable = false

      scene = new Scene(800, 600) {

        fill = LightGreen // con css

        val roleLbl = new Label("Ruolo")
        val roleFld = new TextField()

        val descriptionLbl = new Label("Descrizione")
        val descriptionFld = new TextField()

        val saveBtn = new Button("Salva")
        val clearBtn = new Button("Pulisci")
        val exitBtn = new Button("Esci")


        /*
        root = new StackPane {
          children = new Label("Hello, ScalaFX with JFXApp3!")
        }
        */
      }
*/
/*
    Option(getClass.getResourceAsStream("/img/message.jpg"))
      .foreach(stream => stage.icons.add(new Image(stream)))
*/
//    stage.show()

@main def tryRoleAddView: Unit =
  println("pippo")

*/
